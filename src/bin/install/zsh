#!/bin/zsh

set +x
set +e

# "Safely" Append Line Function

addLine(){
    FILE=$1
    LINE=$2
    touch $FILE && grep -q -- "$LINE" $FILE || echo "$LINE" >> $FILE
}

# Append Configuration

addLine ~/.zshrc "export {https,http}_proxy=localhost:3128"
